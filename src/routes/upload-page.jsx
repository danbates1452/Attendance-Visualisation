export default function UploadPage() {
    return(
        <div>
            Upload Data
            TODO: Link into excel_import.py via API (write a post function)
            Should really be part of Data Management page
        </div>
    );
}