export default function DataPage() {
    return(
        <div>
            DATA MANAGEMENT UI

            &&
            Upload Data
            Link into excel_import.py via API (write a post function)
        </div>
    );
}